import { useNavigation } from '@react-navigation/native';
import { Alert, View } from 'react-native';

interface AvatarProps {}

const KwAlert = ({}: AvatarProps) => {
  return <View />;
};
export default KwAlert;
